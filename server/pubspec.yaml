name: server
description: Game Server
version: 1.0.0

environment:
  sdk: ^3.12.0

dependencies:
  common:
    path: ../common
  shelf: ^1.4.2
  shelf_router: ^1.1.2
  shelf_web_socket: ^3.0.0
  web_socket_channel: ^3.0.3

dev_dependencies:
  http: ^1.2.2
  lints: ^6.0.0
  test: ^1.25.6
